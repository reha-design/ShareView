# Changelog

## 개발 예정

- **시청자가 보낸 파일을 다른 시청자에게도 전달** — 현재 파일은 채팅과 달리 호스트가 중계하지 않아, 시청자가 보낸 파일은 호스트만 받음 (호스트가 보낸 파일은 모든 시청자에게 전달됨). 코드 위치: `screenShare.js`의 `handleIncomingData()` 파일 처리 부분
- **공유 프레임 수 제한 옵션** — 현재는 제한 없이 브라우저 기본값(최대 30fps, 모니터 해상도 그대로)으로 전송. 호스트가 시청자마다 따로 인코딩하므로 CPU가 인원 수의 한계를 정함 (i9-12900F 기준 추정: 정적 화면 20~30명, 동영상 15~20명). 슬라이드 위주일 때 15fps 등으로 낮추면 시청자당 CPU 부담이 줄어 더 많은 인원 수용 가능. 코드 위치: `startScreenShare()`의 `getDisplayMedia` 옵션
- **공유 모드 선택 (문서 / 동영상)** — CPU·네트워크가 부족할 때 브라우저는 화면 공유의 해상도를 유지하고 프레임을 낮춤(약 2fps까지). 문서엔 적합하지만 동영상은 뚝뚝 끊김. 동영상 모드에서는 영상 트랙에 `contentHint = 'motion'`을 지정해 해상도 대신 프레임을 유지하도록 함
- **(장기) 50명 이상 대규모 인원** — 현재 구조(호스트가 시청자별 개별 인코딩)의 한계. 호스트가 한 번만 인코딩하고 중계 서버가 나눠 주는 SFU 구조로 전환 필요

## 2026-09-15 (4차)

### Added
- 사용법 모달 — 첫 방문 시 자동 표시, 이후 사이드바 상단 "사용법" 버튼으로 열기 (한국어/영어). 보고서: [patch_feature_help_modal.md](markdown/Report/patch_feature_help_modal.md)

## 2026-09-15 (3차)

### Added
- 서버 통합 테스트 (`npm test`, Node 기본 `node:test` 사용) — HTTPS 서빙, 시그널링 전달, Host 목록 API, 점(.) 폴더 비노출 확인
- 서버 포트를 `PORT` 환경변수로 지정 가능 (기본 3000)

### Changed
- **HTTPS로 전환** — 브라우저 화면 공유 API는 HTTPS에서만 동작하기 때문에, 이전에는 서버 PC만 호스트가 될 수 있었음. 이제 같은 네트워크의 다른 PC도 `https://<서버-IP>:3000`으로 접속해 호스트 가능
  - 첫 실행 시 자체 서명 인증서를 `.certs/`에 자동 생성 (만료 시 재생성, mkcert 인증서로 교체 가능)
  - PeerJS 시그널링 서버를 별도 포트(9000)에서 웹 서버와 같은 포트(3000)로 통합 — 인증서 경고를 한 번만 수락하면 됨, Docker 포트도 3000 하나만 사용
  - 기존 `http://` 주소는 더 이상 동작하지 않음 → `https://`로 접속

### Fixed
- **보안**: 정적 파일 서버가 `.git` 등 점(.) 폴더 안의 파일까지 웹으로 내려주던 문제 수정

## 2026-09-15 (2차)

### Added
- "화면 공유 중지" 버튼 — 공유 중에는 시작 버튼 대신 표시
- Viewer가 Host의 공유 시작 전에 접속해 있어도, 공유를 시작하면 자동으로 화면을 받음 (공유 중지 후 재시작도 동일)
- Viewer 쪽에도 참가자 목록 표시 (Host가 변경될 때마다 전송)
- 파일 전송 진행률(%) 표시 (보내는 쪽/받는 쪽 모두), 500MB 크기 제한
- 내 ID를 설정하지 않고 연결(또는 호스트 목록 클릭)하면 랜덤 ID로 자동 접속 후 연결
- 채팅에 포함된 http(s) 링크를 클릭하면 새 탭에서 열림

### Fixed
- **보안**: 채팅 메시지·보낸 사람·파일명을 HTML로 그대로 넣어 다른 참가자가 스크립트를 실행시킬 수 있던 문제(XSS) 수정
- 내가 보낸 채팅이 내 화면에 두 번 표시되던 문제 (Host가 보낸 사람에게도 다시 전송하던 것)
- 첫 메시지가 와도 "메시지가 없습니다" 문구가 남아 있던 문제
- Host가 공유를 멈춰도 Viewer 화면에 마지막 영상이 남아 있던 문제
- ID를 설정하지 않고 연결을 누르면 스크립트 오류가 나던 문제

## 2026-09-15 (1차)

### Added
- 다국어 지원(한국어 기본 / 영어) — 사이드바에서 언어 전환, 선택값은 `localStorage`에 저장되어 새로고침 후에도 유지됨 ([i18n.js](i18n.js))
- 같은 네트워크에서 현재 화면 공유 중인 Host 목록을 "세션 참가"에 자동 표시 — Host ID를 직접 입력하지 않고 클릭 한 번으로 연결 가능 (`server.js`의 `/api/hosts` 등록/조회 API, 4초 간격 자동 갱신)

### Fixed
- 한글 버튼 텍스트("설정", "연결" 등)가 두 줄로 줄바꿈되던 레이아웃 버그 수정 (`button { white-space: nowrap }`)

### Changed
- "Start Sharing" 버튼의 카메라 이모지(🎥) 제거

### Docs
- [markdown/test-scenarios.md](markdown/test-scenarios.md) 테스트 시나리오 문서 추가
